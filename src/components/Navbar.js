import React from 'react';

const Navbar = () => {
    return (
      <nav className="bg-light_yellow text-white w-16 h-screen flex flex-col items-center justify-center">
        {/* Navbar content */}
      </nav>
    );
  };
  
  export default Navbar;