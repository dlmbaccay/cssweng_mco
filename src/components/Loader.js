
export default function Loader({ show }) {
  return show ? (
    <div className="bg-gradient-to-br from-jasmine via-citron to-[#7DD184] min-w-full min-h-screen justify-center items-center">
    </div>
  ) : null;
}
